#include <iostream>
#include <cstdio>
#include <cmath>
#include <vector>

using namespace std;

class WordParts {
   public:
   	int partCount(string, string);
};

int WordParts::partCount(string a, string b) {
	int alen = a.length(), blen = b.length();

	
}